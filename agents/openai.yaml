interface:
  display_name: "CYH Flow"
  short_description: "Plan, build, four-lens review, and goal-backed fixing"
  default_prompt: "Use $cyh-flow to plan, build, run a four-lens parallel review with a master recheck, or run a goal-backed adversarial fix loop for this software task."

policy:
  allow_implicit_invocation: false
