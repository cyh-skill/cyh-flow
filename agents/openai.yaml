interface:
  display_name: "Flow"
  short_description: "Plan, build, review, converge, and run a task pool"
  default_prompt: "Use $flow with maximum useful subagent decomposition in every mode: plan; build or build auto with recorded AI decisions; four-lens review; fix or converge; task-add with automatic screenshot preservation; or independently claim and process stored work with task-run."

policy:
  allow_implicit_invocation: false
