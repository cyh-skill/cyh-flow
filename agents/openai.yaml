interface:
  display_name: "cyh-flow"
  short_description: "Plan, build, auto-converge, review, and run tasks"
  default_prompt: "Use $cyh-flow with maximum useful subagent decomposition to plan; build or build auto with recorded AI decisions; run top-level auto through build and all applicable review and test convergence; perform four-lens review; fix or converge; preserve task-add screenshots; or claim and process stored work with task-run."

policy:
  allow_implicit_invocation: false
