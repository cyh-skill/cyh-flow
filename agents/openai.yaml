interface:
  display_name: "cyh-flow"
  short_description: "Plan, build, auto-converge, review, and run tasks"
  default_prompt: "Use $cyh-flow to handle this software-delivery task with the appropriate mode, maximum useful subagent decomposition, and explicit authorization boundaries."

policy:
  allow_implicit_invocation: false
